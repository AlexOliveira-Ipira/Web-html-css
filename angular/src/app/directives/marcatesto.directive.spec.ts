import { MarcatestoDirective } from './marcatesto.directive';

describe('MarcatestoDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcatestoDirective();
    expect(directive).toBeTruthy();
  });
});
